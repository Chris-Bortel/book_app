<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ( '../../layout/head' ) %>
  </head>
  <body>
    <header>
      <%- include ( '../../layout/header.ejs' ) %>
      <h1>Saved Books</h1>
    </header>

    <section>
      <div>
        <img src="<%= book.image_url %>" alt="" />
        <h2><%= book.title %></h2>
        <h3><%= book.author %></h3>
        <p><%= book.isbn %></p>
        <p><%= book.description %></p>
      </div>
    </section>
  </body>
</html>
